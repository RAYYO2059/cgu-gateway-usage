# 用途分類：標註規程

> 這份檔案定的是**在看到任何結果之前**就該定下來的東西：座標、規則、退回門檻。
>
> 建立：2026-09-06

---

## 一、凍結座標

這五項一旦變動，之前算出來的任何比例都不能再跟之後的比。每一項附一句
「它變了會怎樣」，因為變動本身不會報錯——只會讓兩批數字看起來可以比較。

### 母體指紋

```
63af7d40e051d069dece9f14771eab6be7cb487f25c1937f6ff56ec12eb6919b
```

17,365 個相異內容的 `prompt_text_sha256`，**排序後以 `"\n".join` 串接再取
sha256**。串接方式是定義的一部分：無分隔串接會得到另一個值
（`ec123e0d8d76ae06ae51d95f383eff89347d16b96f9d7c37724e4dbab2b4b8dd`），
兩者都對，但只有一個是這裡說的那個。

母體來自 `runs/2026-09-05T1700_prefilter/classify_lite/prefilter_hits.parquet`
的 `rule == "需送分類器"`，即四條前置規則之後剩下的部分。

**它變了會怎樣：** 分類母數變了，所有以 17,365 或 58,100 為分母的比例全部
失效。而且是靜默的——比例照樣算得出來，只是分母換了人。抽樣當時的母數
是 17,368（舊版 request 層 prefilter），與現在差 3；那 3 筆的差異已經足以
讓「四項相加加上剩餘等於總數」對不起來。

### 長度切點

```
[251, 986, 4799]
```

分位定義：`Q1 <= 251 < Q2 <= 986 < Q3 <= 4799 < Q4`。

這組值是 17,365 個相異內容的 `prompt_text_len` 四分位。**現行母體重算的
結果與抽樣當時逐位相同，零列換分位**——所以固定它沒有損失，而重算會讓
「切點是資料的函數」變成每次都要重新記錄的東西。

**它變了會怎樣：** 24 個分層的成員換人，加權準確率的權重跟著換，而且
`baseline.py` 的門檻也是這個 Q1，基準線會一起飄。

### 聚類參數

| 參數 | 值 |
| --- | --- |
| 門檻（成群的最小相異內容數） | 10 |
| 階梯 L | `[4000, 2000, 1000, 500, 250, 120, 60, 30]` |
| 前綴上限 | 4000 字元 |
| 正規化 | 連續空白收斂為單一空格；連續數字換成單一 `0` |

門檻 10 的選擇有量尺：在**已知答案**的 5,437 筆（四支指紋流水線）上，
門檻 20/10/5 的召回率是 94.10% / 97.37% / 98.62%，而**錯併的絕對數三者
都是 2 筆**（同樣的兩群、同樣各混進 1 筆）。降門檻沒有增加錯併，只增加
要人工看的群數（51 → 103 → 200）。10 是召回率與人工成本的折衷。

**它變了會怎樣：** 群集清單整個換掉，`clusters.parquet` 的 `group_id`
不再指同一批內容，已完成的審閱紀錄全部作廢。門檻 20 的 `C01` 有 987 個
相異內容，門檻 10 的 `A001` 只有 974——**編號看起來對得上，內容不是同一批**。
這也是為什麼編號從 C/D 改成 A/B。

### 群集清單

226 群：`A001`–`A123`（無指紋母體）、`B001`–`B103`（有指紋母體）。
涵蓋 11,657 個相異內容 / 32,429 筆請求；散筆 5,708 / 25,671。

檔案：`clusters.parquet`、`cluster_members.parquet`（repo 外，不含明文）。

代表的選法：**群內內容長度最接近中位數的那一筆**；平手取較短，再平手取
sha256 字典序小的。頭尾兩端容易是異常值，中位數那筆最能代表常態。
選法必須是決定性的，否則中斷續跑時螢幕上的代表會換人，而審閱紀錄
記的還是同一個 `group_id`。

**它變了會怎樣：** 審閱時看到的東西換了，但 csv 裡的 `group_id` 沒變——
事後無從發現判斷依據已經不同。

### 基準線

`src/classify_lite/baseline.py`，門檻 `HUMAN_CHAT_MAX_LEN = 251`。
凍結的預測分布（17,365 個相異內容 / 58,100 筆請求）：

| 預測 | 相異內容 | 請求數 | 內容佔比 | 請求佔比 |
| --- | ---: | ---: | ---: | ---: |
| `not_human_chat` | 11,657 | 32,429 | 67.13% | 55.82% |
| `human_chat` | 3,682 | 18,974 | 21.20% | 32.66% |
| `abstain` | 2,026 | 6,697 | 11.67% | 11.53% |

**它變了會怎樣：** 基準線是分類器要打敗的對象。事後調過的基準線無法排除
是照著分類器的結果調的，那時它就不再是獨立的比較對象。`verify_frozen()`
是用來發現它飄掉的，不是用來更新它的。

### LLM 判定器的提示詞指紋

```
prompt_sha256 = ____________________________________________________________
                （**待填**：判定器第一次跑完之後，把 judge_output.csv 的
                  prompt_sha256 欄填進來。在那之前這一格是空的，而不是
                  隨便填一個——填錯比空著更糟。）
```

指紋涵蓋五樣東西，任一改動都會換一個值：提示詞模板本文、三軸的值域與
定義（取自 `ref/label_dictionary.csv` 與載具的 `FIELDS`／`FIELD_HELP`）、
模型名稱、送進提示詞的中繼資料欄位清單、前綴上限與**送哪一欄前綴**
（`prefix_raw` 或 `prefix_normalized`）。

**它變了會怎樣：** 舊的 `judge_output.csv` 全部作廢，須全量重跑。
不重跑而混用，會讓兩批在不同條件下產生的判定落在同一個檔案裡，
而且事後分不出哪一列是哪一批——`prompt_sha256` 欄存在的目的就是讓這件事
當場看得出來，前提是有人真的去看它。

---

## 二、審閱與標註規則

### 不預判

- **不預填、不顯示建議答案、不排序暗示。** 三個軸的值域列出來給人挑，
  但沒有預設值——預設值會變成錨點，而這一輪要的正是未被錨定的判斷。
- 載具的 `FIELDS` 只有值域沒有 default，`FIELD_HELP` 的每個值都有說明
  但沒有哪一個被標成推薦。有測試在守這件事。

### 空白不是答案

- 不確定要**明寫 `unsure`**，不能留空。空值會變成「跳過」的偽裝，而跳過
  與「我判斷是不確定」是兩件不同的事，事後從 csv 分不出來。
- 要跳過按 `s`（不寫入，下次還會出現）；要離開按 `q`（已審的都已寫入）。

### 審閱順序

1. `A001` 永遠第一——它單獨影響已發表的 `OVERVIEW` 4.2 那組數字。
2. 其餘按 `前綴佔中位內容長度比例` 由高到低。
3. 比例相同時按相異內容數由大到小。

**不是按規模排序。** 比例高的群，外框就是任務本身，看一眼就能判；先審
這些能在碰到模糊案例之前建立對這批資料的常態感。比例低的群外框只是信封，
判斷成本高，留到後面。規模大的群不一定好判——`A001` 有 974 個相異內容，
前綴卻只佔內容的一成，正是最難判的那一類。

### 批次與疲勞

- **一次最多 3 批，每批 10 個單位。** 超過就停，隔一段時間再來。
- 每個單位的耗時由載具自動記錄（`elapsed_sec`）。
- **耗時驟降或 `unsure` 率驟降是疲勞訊號，不是熟練。** 熟練會讓耗時緩慢
  下降並趨於平穩；疲勞是突然變快、而且不再標 `unsure`——因為判斷變成了
  猜測，而猜測比判斷快。看到這個形狀就停，那一批之後的紀錄要重做。
- 每批之間不要回頭改前一批。要改就整批重做並記下理由。

### 疲勞偵測的失效條件

上面兩個訊號都有前提，而前提不成立時它們**不會沉默，會給出「正常」**。

**`unsure` 率驟降的前提是 `unsure` 率非零。** 前綴篩檢那一輪
`unsure` 從頭到尾是 0——沒有可以下降的東西，訊號結構性失效。
**這種失效不會表現為「訊號沒有觸發」，會表現為「訊號正常且無異狀」**，
而兩者在紀錄上長得一模一樣。一個從未觸發的警報與一個不可能觸發的警報，
事後從日誌分不出來。

**耗時驟降同樣不能單獨判讀。** 前綴篩檢的逐批（每 10 群）耗時中位數：

```
29.0  15.2  11.1   5.6   3.0   2.4   2.6   1.8   1.4   2.0
 2.4   1.6   1.7   0.9   1.8   1.2   1.6   1.4   0.8   1.0
 0.8   0.6   3.0
```

形狀是判準建立階段的陡降（前三批），之後在 1–2 秒的平台上震盪，
**最後一批反彈到 3.0**。疲勞不會在最後一批反彈，所以這條曲線比較支持
熟練而非疲勞——**但「比較支持」不是證據**。同一條曲線也相容於
「後半段判得草率、最後一批剛好遇到幾個難的」。

**規則：**

1. 任一疲勞訊號在使用前**必須先確認它的前提成立**。
2. 前提不成立時要在該輪的紀錄裡**明寫「本輪此訊號不可用」**，
   不可當成通過。
3. **唯一不依賴前提的檢查是盲重標的一致率**（見〈一致性抽驗〉與
   〈複核抽樣〉），所以它**不可省略**。前兩個訊號是便宜的早期警報，
   一致率是唯一的驗收。

### 一致性抽驗

- 每完成 5 批，從已審的群裡隨機抽 10% 重審一次（不看第一次的答案）。
- 兩次的一致率就是下一節的「自我一致率」。

### 盲性

人工審閱**不得在看過 LLM 判定之後進行**，且載具在任何模式下都不得顯示
LLM 的判定結果。現行載具讀 `clusters.parquet` 而非 `judge_output.csv`，
**這是設計不是巧合，不要為了方便而把兩者接起來。**

一旦人看過 LLM 的答案，該群的人工判定就不再是獨立參照，一致率也就失去
意義——它會變成「人有多容易被說服」的量測，而那個數字看起來與真正的
一致率完全一樣，事後也分不出來。

同理，`judge_clusters.py` 也不讀 `cluster_review.csv`。兩邊各自只讀
自己需要的東西，是為了讓「沒有互看」這件事由檔案依賴關係保證，
而不是靠人記得。

### 分類法的定義不得從模型的判定結果導出

〈盲性〉管的範圍是**單一群**：人看過某群的 LLM 判定，該群的人工判定就不再是
獨立參照。**字典在更上游。** 一本從模型行為抄來的字典，會讓之後**每一群**的
人工判定都不獨立，而算出來的一致率與真正的一致率**在數字上完全一樣**。

**本專案的實例。** 對話端曾要求從 69 筆模型判定（三批 × 23 群）導出兩條邊界
定義。那 69 筆呈現一個很乾淨的規律——`user` 外框從未被判 `split`（0/42）、
`tool` 外框從未被判 `keep`（0/27），例外只有 4 筆。**規律乾淨正是危險的地方**：
它看起來像一條現成的邊界，寫進字典之後判定器與人工審閱會高度一致，而那個
一致率量到的是「人有沒有照著一本從模型行為抄來的字典作答」。由執行端擋下。

規則：

- **(a) 分類法的邊界從內容寫，不從判定結果的規律寫。** 中繼資料可以用來
  排除候選判準（「這個軸分不開，不用再看」），不可以用來產生判準。
- **(b) 用來導出邊界的群，不得用於驗證該邊界。** 那是自我驗證。
- **(c) 驗證優先用完全未判定的群。** 其次是已判但未參與導出的群。

**已知的殘留污染，分兩級。**

- **整個測試台 A001–A023 都是導出材料。** 那次導出用的是 69 筆判定
  ＝三批 × 23 群，所以 (b) 適用於**全部 23 群**，不是只適用於不穩定的那七群。
- **其中七群另有更強的污染。** A008／A010／A013／A017／A018／A019／A020
  是**因模型不穩定而被選出**——選擇本身就帶有模型的資訊，與看不看得到
  判定值無關；而它們的三次判定值也已經被列出。**這七群的盲性不可恢復。**

**驗證要用尚未判定的群**：完全未判定的 118 群是首選，只在舊指紋下判過的
84 群是次選。切分、涵蓋率與其限制見第三節〈驗證涵蓋率的限制〉。

（本節第一版寫的是「驗證要用另外 16 群與尚未判定的 202 群」。**兩處都錯，
而且錯的方向都是放寬。** 那 16 群同樣是導出材料，何況它們是「判定穩定的
那些」——拿它們驗證等於先對穩定性做了條件化，一致率會被墊高；而「202 群
尚未判定」把 84 個在舊指紋下判過的群算成了乾淨的。由第三節 (d) 的措辭與
本節對不上而查出：**同一份檔案裡兩處講同一件事而說法不同，是查證的入口**。）

（記下來的理由：不可恢復的污染若沒有寫在案上，下一輪會有人拿它們算一致率，
而那個數字會很好看。）

### 「AI 不得執行」的適用範圍隨閘門狀態改變

三支 scratchpad 腳本原本一律標「AI 不得執行」，理由是明文會進 transcript。
**理由消失時要明改規則，不要靠下指令的人記得例外。**

- `judge_clusters.py`：理由已不成立，改為**有條件允許 AI 執行**。
  前綴通過人工閘門（225 clean / 1 flagged，自我一致率 20/20）之後，
  它送出去的就是已篩檢的前綴；而它不 print 前綴也不 print reason，
  該性質由 `OutputGuard` 機械保證（包住 stdout 與 stderr，把本群前綴與
  reason 當禁字，24 字元重疊即中止）。三條條件寫在該檔檔頭。
- `review_clusters.py`、`rescreen_prefixes.py`：**不變**。
  它們會把完整內容印到螢幕，理由仍然成立。

本專案曾發生對話端直接下令執行而未先改規則，由執行端擋下。擋下來是對的，
但**真正該修的是規則本身**——一條理由已經消失的禁令，每次都要靠人臨場
判斷要不要破例，而破例的判斷不會留下紀錄。條件寫進程式、範圍寫進本節，
兩邊都不依賴記性。

### 複核抽樣

LLM 判定完成後，人工**盲審 40 群**作為複核樣本。

- **分層抽樣**，分層維度是 `prefix_len_raw`（切點取該欄分布的三分位）。
  **不用 `confidence`。**

  原本寫的是 `confidence` × `共同前綴長度_原始`。改掉的理由，量出來的：

  - `confidence` 在 130 筆判定裡**沒有出現過任何 `low`**，值域三檔只用了
    兩檔，而 `medium` 佔 80.4%。**一層裝 80% 的樣本不是分層。**
  - `prefix_len_raw` 的層是連續可切的，而且**在判定之前就已知**——
    不必先跑完整批才知道要抽誰。
  - 它與 `confidence` 相關 ρ=+0.474（p<0.0001）、與 `axis_level` 相關
    ρ=+0.334（p=0.0004）：原本想用 confidence 分出來的差異，長度分得出來。
  - **不是假相關。** 控制 `axis_level` 之後長度效應仍在
    （`all_three` 內 ρ=+0.592、`invocation_only` 內 ρ=+0.290）。

  `confidence` 保留為**事後驗證欄**，不作為抽樣依據——它可以用來回答
  「模型自陳有把握時是不是真的比較準」，但它不能決定抽誰。

- **分別報各層一致率，不只報整體。** 長前綴層的準確率與短前綴層的準確率
  是兩個不同的問題：前者問「材料充足時它對不對」，後者問「材料不足時它是
  不是知道自己不該有把握」。混在一起算沒有診斷力——一個 0.80 的整體一致率，
  可能是各層都 0.80，也可能是長前綴 0.95、短前綴 0.50，而這兩種情況該做的
  事完全不同。
- **抽樣腳本進版控**，**種子與母體指紋一併記錄**。理由同本檔第四節：
  事後才決定抽哪 40 群，無法排除是照著結果挑的。

### 判定器的重跑條件

**`prompt_sha256` 改變即舊結果不可比，須全量重跑。** 下列任一改動都會
換指紋：

- 提示詞模板本文
- 三軸的值域或定義（`ref/label_dictionary.csv`、載具的 `FIELDS`／`FIELD_HELP`）
- 模型名稱
- 送進提示詞的中繼資料欄位清單
- 前綴上限，或送 `prefix_raw` 改成送 `prefix_normalized`

**不可以只重跑一部分。** 混用兩個指紋的結果，等於在同一張表裡放兩個
不同實驗的觀測值。要比較兩種設定的話，跑兩份輸出檔各自完整，不要就地
覆蓋。

---

## 三、退回門檻

**這一節在看到任何結果之前定下。** 事後訂的門檻無法排除是照著結果訂的。

| 條件 | 處置 |
| --- | --- |
| 自我一致率 < 0.80（任一軸） | **退回改字典，不跑分類器。** 人自己都對不上，模型對得上也沒有意義 |
| `UNCERTAIN` 率 > 20%（任一軸） | 該軸**不出比例，只做描述**。分母裡有兩成是「不知道」時，比例的誤導性大於資訊量 |
| 分類器加權一致率 < 0.70 | 該軸**不進報告** |
| 0.70 – 0.85 | **只出粗分類**（合併相近的值），不出細項比例 |
| ≥ 0.85 | 可出比例，**仍附混淆矩陣** |
| 任一類別 n < 10 | **只出計數不出比例**（沿用 `config.MIN_GROUP_SIZE`） |
| 分類器未勝過 `baseline.py` | 該軸的內容分類**不算有貢獻**，報告中據實說明 |

「加權」指以 24 個分層在母體裡的佔比加權。**四個分層在 150 筆舊驗證集裡
沒有樣本**（`服務憑證|Q3|重複`、`教職員|Q4|重複`、`服務憑證|Q4|重複`、
`服務憑證|Q4|單次`，合計 145 個相異內容、佔母體 0.84%）——那 0.84% 的加權
準確率是**無定義**，不是 0。新抽樣要處理這件事。

「勝過基準線」的定義：在同一組人工標註上，分類器要麼在 `abstain` 那
2,026 筆（佔母體 11.67%）上真的判得出東西，要麼在另外兩桶上比基準線更準。
兩者都做不到，就代表這一輪的內容分類沒有帶來新資訊——**那是個結論，
不是失敗**，而且是個省錢的結論。

### 字典修訂後的效果驗證（判讀規則寫定於重跑之前）

**這一節在重跑之前寫定，時間戳為證。** 判讀規則若等看到結果再訂，那訂出來的
是對結果的解釋不是對結果的檢定——而兩者寫下來長得一模一樣。

- **2026-09-07 初版**：方案是「字典修訂後同指紋重跑 A001–A023 測試台，
  量自我一致率」。
- **2026-09-08 改版**：**該方案作廢**，理由見下。**作廢的方案連同理由一併
  留著，不刪。** 一份預先寫定的規則若可以被安靜換掉，它就不再是預先寫定的
  ——留著才看得出換過，也才看得出換的理由是不是事後配合結果編的。

#### 作廢的方案：23 群測試台重跑量一致率

四個理由，任一個單獨就足以作廢。

**(a) 判定器看不到字典。** 判定器的三軸定義取自 `review_clusters.py` 的
`FIELD_HELP`（硬編碼），**不讀 `ref/label_dictionary.csv`**。只改後者然後
重跑，量到的是純雜訊——**而純雜訊看起來就像「字典沒效果」**，兩者在數字上
無法分辨。釘住它的是 `test_判定器不讀_label_dictionary`；那個測試存在的理由
是這件事**看起來應該不成立**（檔案叫「標籤字典」，直覺會以為判定器讀它）。

**(b) 要讓判定器看到就得改 `FIELD_HELP`，而 `FIELD_HELP` 在指紋裡。**
所以「字典改後」那幾次**必然是新指紋**，與基準線的比較**必然是跨指紋比較**。
這不是安排失誤，是這個測量的定義本身——**字典就是提示詞的一部分**。
釘住它的是 `test_三軸定義改了指紋就會變`。

**(c) 跨指紋的雜訊剛好等於判讀門檻。** 跨指紋雜訊底線實測 8.7pp
（`disposition` 的兩次跨指紋比對，一次 73.9% 一次 82.6%），而 n=23 的判讀
門檻正好是 8.7pp（一群 4.3pp，變動要跨兩群才讀得出結論）。**兩個數字相等
不是保守估計，是巧合地剛好相等**——n=23 驗不動小效果。擴到 46 群門檻降到
4.35pp，仍被 8.7pp 的擺動蓋過；要穩妥驗一到兩群的效果需要 69 群，代價
$6.88——**比跑完剩下那 202 群（$4.01–6.05）還貴**。

**(d) 那 23 群本來就不可用於驗證。** 第二節〈分類法的定義不得從模型的判定
結果導出〉的 (b)：用來導出邊界的群不得用於驗證該邊界。七群是導出材料。
**就算 (a)(b)(c) 都能解決，用它們驗證仍然是自我驗證。**

#### 現行方案：跑尚未判定的群，比分布

字典修訂後直接跑尚未判定的群，與現有判定比**三軸的分布**，
**不做逐群成對比較**。

理由：

- 未判定的群符合第二節 (c)，是乾淨的驗證集；測試台不是
- n 夠大時解析度遠低於跨指紋擺動：n=118 一群 0.85pp、n=202 一群 0.50pp。
  **8.7pp 的擺動在這個尺度上是可以量的量，不是蓋掉一切的底噪**
- 分布比較不需要配對，所以「基準那次是舊指紋」不構成問題——
  逐群成對比較才需要同指紋

#### 「202 群完全未判定」不成立，要分兩層

225 個 clean 群的實際狀態（2026-09-08 查證）：

| 集合 | 群 | 相異內容 | 請求 | 狀態 |
| --- | --- | --- | --- | --- |
| 測試台 A001–A023 | 23 | 4,422 | 20,249 | 判過三次。**導出材料，不可用於驗證** |
| 僅 `79635e2b` 判過 | 84 | 1,776 | 4,304 | 判過一次（舊指紋）。第二節 (c) 的**次選** |
| 完全未判定 | 118 | 5,444 | 7,826 | 第二節 (c) 的**首選** |
| clean 合計 | 225 | 11,642 | 32,379 | |

**「剩下 202 群」= 225 − 23，是「以現行指紋為準的未判定數」，不是
「完全未判定」。** 其中 84 群在 `judge_output.79635e2b.csv` 裡已經有一次
判定。本檔一度把 202 寫成「完全未判定、是唯一乾淨的驗證集」，由執行端查出。
（同一類錯誤第二次：分母的定義隨指紋而變，而「未判定」這個詞不帶指紋。）

- **主結果用 118 群**（首選，解析度 0.85pp）
- **84 群一起跑，但分開報。** 它們是次選；同時它們多帶一件東西——
  有舊指紋的配對判定，可以順便量一次「跨指紋＋字典」的成對變動。
  **那是加值，不是主結果**，因為那兩次之間有兩個改動而不是一個

#### 要比的分布

`frame_owner`、`disposition`、`axis_level` 三軸各自的：**群數、涵蓋的相異
內容數、涵蓋的請求數**。

**三個都要報。** 它們的加權完全不同：測試台的 23 群只佔 clean 群數的
10.2%，卻佔請求數的 62.5%。只報群數會把一群 20,249 筆請求的結構當成
一群 5 筆的同等份量。

**逐軸報，不合併。**

| 觀察 | 結論 |
| --- | --- |
| `frame_owner` 分布不動、`disposition` 的 `user`→`remove` 上升 | 兩條邊界生效 |
| `frame_owner` 分布也動了 | 字典連帶改了「外框是誰寫的」的判準。`disposition` 的變動**不可單獨歸因於**那兩條邊界 |
| 兩軸都不動 | 邊界未被判定器採用，或母體裡本來就罕見——**本設計分不開**，見下 |

合併報會蓋掉這個區分，而三種情況該做的事完全不同。理由是量出來的：
A017 與 A019 的 `disposition` 是**跟著 `frame_owner` 一起翻**的
（`user`↔`tool` 換一次，`keep`↔`split` 就跟著換一次）。字典把兩條邊界寫得
再清楚，只要 `frame_owner` 還在跳，`disposition` 就跟著跳。

#### 預期方向（寫定於重跑之前）

**第一條邊界（使用者自己寫的固定外框）。** 基準值，實測 2026-09-08：

| 集合 | `user` 判定次數 | 其中 `remove` |
| --- | --- | --- |
| 測試台 69 筆（23 群 × 3 批） | 42 | 4（9.5%） |
| 84 群（僅 `79635e2b`） | 53 | **0** |
| 130 筆合計（107 群） | 80 | 3 |

**「4/42 這種零星狀態」是測試台的數字，不是母體的。** 在測試台以外，
`user`→`remove` **一次都沒有出現過（0/53）**；130 筆裡那 3 筆全部落在
A008、A010、A018——正是那三個因不穩定而被選出的污染群。

這件事改變預期方向能怎麼寫：

- **「收斂到 0」不可證。** 乾淨資料上它**已經**是 0，字典再怎麼寫都還是 0。
  這個方向沒有判別力，不能當判準。
- **可讀的只有「明顯上升」，而且要用比例不是群數。** 基準 0/53 的 95% 上界
  依三法則是 3/53 = **5.7%**。判準：118 群裡 `user`→`remove` 的比例
  **超過 5.7%** 才算字典生效。（118 群裡有幾群是 `user` 要跑完才知道；
  若按 53/84 = 63.1% 推約 74 群，5.7% 約當 4.2 群。）
- **若仍然是 0，有兩種解釋而本設計分不開**：邊界寫了但判定器不採用；
  或者「使用者模板」在母體裡本來就罕見——測試台是照不穩定挑的，
  本來就會富集這一類。要分開需要先知道母體裡有多少群是使用者模板，
  而那要從內容看，不是這一輪能回答的。**分不開就明講分不開。**

**第二條邊界（一群混著兩種呼叫形式時 split 還是歸給主要那一種）。**
它影響的是 `tool` 群的 `split` 比例，基準是 84 群的 14/30 = 46.7%
（另有 `remove` 13、`keep` 3）。**預期方向要等字典寫完才寫得出來**——
方向取決於字典選哪一端，寫成「split 比例會變」等於沒有預期。
**但它必須在跑之前補上**，補的時候沿用本節的格式與時間戳規則。

#### 代價（估算，2026-09-08）

token 用 148 筆單輪實測擬合 `prompt_tokens ≈ 0.379 × min(前綴長, 1200) + 1,819`
（殘差 sd 85）。成本下界用快取熱的 $0.0190／次、上界用快取冷的 $0.0291／次，
盲檢探針 $0.0187／次（啟動一次，之後每 25 群一次）。

| 集合 | 群 | 探針 | 呼叫 | 估 prompt tokens | 估 USD | 一群 |
| --- | --- | --- | --- | --- | --- | --- |
| 完全未判定 118 | 118 | 5 | 123 | 222,078 | 2.34–3.53 | 0.85pp |
| 僅 `79635e2b` 判過 84 | 84 | 4 | 88 | 162,873 | 1.67–2.52 | 1.19pp |
| 兩者合計（＝「202」） | 202 | 9 | 211 | 384,951 | 4.01–6.05 | 0.50pp |

#### 驗證涵蓋率的限制（寫在跑之前）

乾淨驗證集只有 118 群，而**請求量集中在已污染的那 23 群**。

| 集合 | 相異內容 | 佔分類母數<br>17,365 | 請求 | 佔分類母數<br>58,100 | 佔 clean<br>32,379 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 測試台 23（已污染） | 4,422 | 25.5% | 20,249 | 34.9% | **62.5%** |
| 僅舊指紋 84 | 1,776 | 10.2% | 4,304 | 7.4% | 13.3% |
| 完全未判定 118 | 5,444 | 31.4% | 7,826 | 13.5% | **24.2%** |
| clean 225 合計 | 11,642 | 67.0% | 32,379 | 55.7% | 100% |

**分母要跟著數字走。** 「驗證涵蓋 24.2% 的請求」的分母是 clean 群的
32,379；改用**分類母數**的 58,100 當分母只剩 **13.5%**。兩個都對，
講的不是同一件事，**不標分母就不可以並排**——與
`docs/ENGINEERING_NOTES.md`〈並排的數字要標明各自的算法〉同族：
那一條管算法，這裡管分母，失效方式一樣。

**含意分兩層，報告時不得混用。**

- **驗證能力。** 字典效果在 118 群上量得到，但那是**小群長尾**：
  平均 46 個相異內容（測試台平均 192，4.2 倍），請求數中位 22
  （測試台中位 171）。**以請求加權的效果在乾淨資料上驗不出來**——
  clean 前三大群 `A001`（7,503 筆）、`A002`（4,753）、`A013`（2,687）
  **全部在測試台裡**；118 群裡最大的是 `B002`，1,940 筆。
- **產出能力。** 那 23 群**仍然會被判、仍然進最終分類母數**。
  **污染破壞的是驗證能力，不是產出能力**——判定值照樣可用，
  只是不能拿它們來證明字典有效。

所以最終報告要能**分別陳述**兩件事，而且不得互相代替：

1. **分類母數是多少**——含那 23 群與它們的 20,249 筆請求
2. **字典效果的驗證涵蓋了多少**——118 群、7,826 筆請求，
   佔 clean 的 24.2%、佔分類母數的 13.5%

**這個限制不是 n 不夠，加樣本解決不了。** 是**大群幾乎全在污染側**：
即使把 84 群也算進來，最大的仍然只有 424 筆。要恢復請求加權的驗證能力
只有一條路——拿沒判過的大群來驗，而 `B002` 的 1,940 筆就是上限。
若之後要專門補這件事，做法是**按請求數分層再抽**，而那必須在跑之前決定，
不是看完結果再補（第三節開頭那句話同樣適用）。

**成因不是「大群比較容易不穩定」，是編號順序。** 群編號按相異內容數遞減
指派：A 系列 ρ=−0.999 且**完全單調**，B 系列 −0.998。所以「先跑 A001–A023」
＝「先跑 A 系列最大的 23 群」。**那不是抽樣，是取排序後的前綴。**

曾經想的成因是「大群內容多、異質性高，較容易讓模型不穩定，於是選擇偏誤
順帶把大群一起帶走」。**量過，不成立**：測試台裡不穩定的七群相異內容數
中位 80，穩定的十六群中位 140——方向是反的，而 Mann-Whitney p=0.160
（請求數 p=0.841）。**n=7 對 16，兩個方向都下不了結論**，只能說
「這批資料分不出群大小與判定穩定性有關」。依
`docs/ENGINEERING_NOTES.md`〈差小於量測解析度時，不要從方向讀出結論〉。
通則見同檔〈「取前 N 個」不是抽樣，除非你知道排序規則〉。

**「大群」要標明是哪一種大。**

| 最大的群 | 測試台 23 | 完全未判定 118 |
| --- | --- | --- |
| 相異內容數 | `A001` 974 | **`B001` 1,282**（全體最大） |
| 請求數 | **`A001` 7,503** | `B002` 1,940 |

**請求量集中在測試台，相異內容數不集中。** 測試台佔相異內容 25.5%、
佔請求 34.9%，差別來自每個內容被重送幾次（`A001` 7.7 次／內容，
`B001` 1.0）。所以上面那句「大群幾乎全在污染側」**只在請求加權下成立**；
以相異內容為單位，最大的那一群反而在乾淨側。

這兩件事合起來的含意：**要驗以內容為單位的效果，118 群夠用；
要驗以請求加權的效果，驗不動。** 報告分開講的兩個數字（分類母數／
驗證涵蓋）也要照這個單位分開。

#### 附：測試台的自我一致率基準（保留為 (c) 的來源）

A001–A023，n=23。**兩種算法要分開記，不可混用：**

| 軸 | 三次全同（穩定普查） | 同指紋兩次（雜訊底線） |
| --- | --- | --- |
| `frame_owner` | 91.3% | 91.3% |
| `disposition` | 69.6% | 82.6% |
| `axis_level` | 78.3% | 87.0% |
| `confidence` | 69.6% | 91.3% |

- **三次全同**是穩定普查，用途是選出不穩定的群（那七群就是這樣選的）。
- **同指紋兩次**是雜訊底線，(c) 的 8.7pp 就是拿它跟跨指紋的 73.9% 比出來的。

**這兩欄不可以互相取代。** `frame_owner` 兩欄剛好都是 91.3% 是**巧合**：
三次裡不穩的只有 A017、A019 兩群，而這兩群**在 `a0d19daa#1` 與 `#2` 之間
就已經不同**，所以成對比較抓到的是同樣那兩群。

**不是「異值都落在同一批」**——A017 的異值在 `#1`（`user`／**`tool`**／`user`），
A019 的異值在 `#2`（`tool`／`tool`／**`user`**），兩群的異值在不同批。
若某群的不同只出現在 `79635e2b`，兩欄就會分開；`axis_level` 差 8.7pp、
`confidence` 差 21.7pp 就是這樣來的。

本檔第一版把 `axis_level` 與 `confidence` 填成成對的值、`disposition` 填成
三次的值，**四個數字裡兩個對兩個錯，而它們並排在一起看不出來**——由測試
抓到（`test_協定記的基準一致率與實際輸出相符`）。通則見
`docs/ENGINEERING_NOTES.md`〈並排的數字要標明各自的算法〉。

**解析度：n=23，一群 = 4.3pp，變動要跨兩群（8.7pp）才讀得出結論。**
見 `docs/ENGINEERING_NOTES.md`〈差小於量測解析度時，不要從方向讀出結論〉。

### 自我一致率是必要條件，不是充分條件

**它排除隨機性，不排除系統性偏誤。** 單一標註者的場合，
**「一致率 100%」與「判準一致地錯」在數字上無法分辨**——偏誤是穩定的，
重測不會動搖它，一致率這個量測對它完全無感。

因此**任何以自我一致率為依據的通過判定，都要同時記載「正確性由什麼
驗證」**。可能的答案有幾種，寫哪一種都可以，但不可以空著：

- 第二標註者的跨標註者一致率
- 一組有已知答案的植入樣本
- 下游的獨立檢查（例如分類器與人工在同一批上的一致率）
- **「無法驗證」**——這也是合法答案，但要**明寫無法驗證及其結構性理由**，
  不得以一致率代替。

「無法驗證」與「還沒驗證」是兩件事，紀錄上要分得出來：前者是決定，
後者是待辦。

**本專案的第一次適用：前綴篩檢。** 20/20 通過（逐層皆 100%，含最可疑的
≤1 秒層），正確性**無法驗證**——外部檢查只有引入第二標註者一途，而本批
資料含第三人個資與臨床照護紀錄，多一個人看過 226 段前綴就是多一個人
接觸那些內容：**驗證動作本身會製造它要防的風險**。詳見 `DESIGN_NOTES`
〈明文邊界的唯一例外〉底下的〈20/20 證明了什麼、沒證明什麼〉。

連帶的報告限制：`flagged` 率 1/226 出現在任何對外文件時，必須帶著
「單一標註者、正確性未經外部驗證」的限定。**它的用途是說明「這件事會
發生」，不是估計發生率。**

---

## 四、為什麼這份檔要進版控

門檻的可信度來自它**在資料之前就存在**。

一個 0.85 的門檻，若是在看到分類器跑出 0.87 之後才寫下來的，它就不是門檻，
是事後的合理化。而從結果上看，兩者長得一模一樣：同樣的數字、同樣的措辭、
同樣的「我們早就決定了」。**沒有任何方法能從文件內容分辨這兩者。**

git 的時間戳是唯一的證據。這份檔案先於標註與分類進版控，之後任何人
（包括寫它的人）都可以用 `git log` 確認它不是倒填的。

同樣的理由，這份檔案**不放在 `docs/DESIGN_NOTES.md`**（那份不進版控），
也不寫在對話紀錄裡（那些會消失，而且無法佐證先後）。

`ref/` 是白名單制（`ref/*` 擋全部、逐項放行），所以這份檔案需要在
`.gitignore` 裡明確放行才進得了版控——被 ignore 的檔案不會出現在
`git status`，會安靜地留在本機直到有人 clone 之後點不到。

---

## 附：相關檔案

| 檔案 | 位置 | 進版控 |
| --- | --- | --- |
| 本規程 | `ref/annotation_protocol.md` | 是 |
| 分類字典 | `ref/label_dictionary.csv` | 是 |
| 前置規則判定字串 | `ref/prefilter_markers.csv` | 是 |
| 基準線 | `src/classify_lite/baseline.py` | 是 |
| 群集清單 | `_rescued_scratchpad/clusters.parquet` | 否（repo 外） |
| 群集成員 | `_rescued_scratchpad/cluster_members.parquet` | 否（repo 外） |
| 審閱載具 | `_rescued_scratchpad/review_clusters.py` | 否（會顯示明文，格式未定案） |
| 審閱紀錄 | `_rescued_scratchpad/cluster_review.csv` | 否 |

載具與紀錄留在 repo 外，理由是**顯示明文的那一步只能在人的終端進行**：
任何經過 AI 的 tool output 的東西都會寫進 session transcript，等於明文落地，
而日誌內容含臨床照護紀錄。載具的邏輯正確性由
`tests/test_review_clusters.py` 以假資料驗證。
