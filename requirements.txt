pandas
pyarrow

# schema.py 用的是 `import pandera.pandas as pa`，這個 import path 是 0.24
# 之後才有的（更早的版本是 `import pandera as pa`）。裝到舊版會 ImportError，
# 而錯誤訊息只會說找不到 pandera.pandas，完全不提示是版本問題。
pandera>=0.24

# Windows 沒有系統時區資料庫，zoneinfo 需要這個套件才能解析 Asia/Taipei
tzdata
matplotlib
