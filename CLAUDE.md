# 專案基準

> 這份檔案是本專案的入口。對話端的 AI 與機器上的編碼代理都讀它。
>
> **它不是紀錄，是入口。** 收錄的判準只有一條：**一個不知道任何歷史的人或
> 模型，讀完之後會不會在第一個小時做出錯的決定。** 會，就寫；不會，就不寫。
> 歷史敘述在 `docs/DESIGN_NOTES.md`，不在這裡。
>
> 內容有變動就當下修。帶著過期的基準往下做，錯誤會擴散到之後每個決定。
>
> 最後更新：2026-09-11

---

## 這是什麼

CGU 有一個 AI Gateway（air.cgu.edu.tw），全校師生透過它呼叫 OpenAI 等模型。
校方問三件事：**各學院／職員在用什麼、用來做什麼、花了多少錢。**

這三個問題在日誌裡都沒有直接答案——身分被雜湊、沒有用途欄位、沒有金額欄位。
所以整個專案是重建，不是查詢。

**核心原則：難處不在算出數字，在讓人知道數字能信到什麼程度。**

repo：https://github.com/RAYYO2059/cgu-gateway-usage

---

## 目前狀態

三個問題交了兩個。

| 問題 | 狀態 |
| --- | --- |
| 誰在用 | 完成。最小可靠粒度是**學院**，不是個人 |
| 花多少 | 完成。US$2,749.32，牌價等值非帳單，**且非全量**（見下） |
| 做什麼 | **未完成**。225 個 clean 群的分流判定已跑完；值域／判準尚未收斂，內容分類未開始 |

`docs/OVERVIEW.md` 是主要交付物，七節散文全部寫完、數字全部逐項查證過。

### 往下讀之前，三件不要做

這三件都符合「檔案在、看起來像成果、實際不可用」，所以會在第一個小時發生。

**(a) 不要重抽、也不要沿用那 150 筆驗證集。**

- 它已遺失且**不可重現**。抽樣程式 `valset.py` 還在 `_rescued_scratchpad/`，
  但它吃的是舊的 request 層 prefilter 母數 **17,368**，而現行母數是
  **17,365**——差 3 筆，足以讓分層與抽中的樣本全部換人。
- `docs/CLASSIFICATION_NOTES.md` 裡那 150 列逐筆觀察**仍然在**，內容也仍然
  有參考價值，**但它的編號對應一個已不存在的抽樣**，與任何現行產物都對不上。
  拿它去核對現在的群編號會得到一組看起來合理的假對應。
- **現行路線不是逐筆抽樣，是群集判定**：把相異內容聚成群，判「這一群要不要
  進分類母數、能判到哪一層」，再對留下來的群做內容分類。

**(b) 不要把不同提示詞指紋的 `judge_output*.csv` 當成同一批。**

- 現行檔是 `judge_output.e13f9738.csv`：225 個 clean 群各一列，完整指紋
  `e13f9738fba32a8f5167807157718758954dc2e2f1304392bdf86b3b7dd97114`。
  這是唯一可當現行全群**描述性模型結果**的檔案；它沒有獨立參考標籤，
  不得寫成已驗證分類。分母與但書見 `docs/INDEX.md`。
- `79635e2b…`、`a0d19daa…`、`a2dc7b11…`、`83086cd2…` 全部是舊指紋。
  它們只可當歷史量測或配對材料；**不可接到現行檔後面補列，也不可把舊值
  當成現行結果。**
- 226 群裡另有 1 群 `flagged`，依前綴篩檢閘門不送判定器。所以「全數有
  交代」是 **225 群判定 + 1 群排除**，不是 226 列判定。

**(c) 225 群雖然已有同指紋判定，不是驗證集。**

現行檔必須按既定來源拆成三個 cohort：

- **118 群**：已三度用於調參，現在是開發資料，不是驗證集。
- **次 cohort 84 群**：有舊指紋的配對判定，只能分開報。
- **A001–A023 共 23 群**：導出材料／測試台，其中七群更帶有模型選擇資訊；
  盲性不可恢復，只能當判準盤點材料。

三者合計 225。**全體分布只能當開發階段的描述性材料，不能拿來當驗證結論。**
取得新人工盲標資料前的 taxonomy、提示詞、本地請求設定指紋、評估指標與
退回門檻已凍結在 `ref/FREEZE_2026-09.md`；人工盲標的判讀規則也已在標註
開始前補入同一份凍結檔，之後不回改，任何新變更另開 FREEZE。

### 現行分流結果（2026-09-11，225 clean 群）

`disposition`：`batch_project` 163、`user_envelope` 37、`tool_injected` 13、
`service_relay` 11、`insufficient` 1。三條 must 約束均 0 違反。default 兩組
各至少有一個方向低於 90%，依預先登記門檻，**這一批判定不可信**；不得用
事後解釋改變這個結論。

這批結果**沒有讓現行判準自行成立**：

- 四個既知 `tool_injected` 候選裡，A002、A013 命中；A001、A036 被判成
  `user_envelope`。13 個實際 `tool_injected` 群的 uid 數中位數是 2，
  unit_type 種類中位數是 1，與「uid ≥ 10 且 unit_type 種類 ≥ 3」不一致。
- 原本 107 群裡有 24 群的隱藏上下文請求佔比大於 0；其中沒有一群被判成
  `insufficient`（16 `user_envelope`、5 `batch_project`、3 `tool_injected`）。
- 163 個 `batch_project` 群裡 88 群 uid 數 ≤ 3，符合率 54.0%；仍與判準相反。
- 全體 default 共現率不可蓋過 cohort：`batch_project → all_three` 139/163
  （85.3%），反向 139/146（95.2%）；`user_envelope → invocation_only` 30/37
  （81.1%），反向 30/54（55.6%）。依預先登記門檻，前者各有一個方向、
  後者兩個方向低於 90%。**這裡只記結果；新的值域與判準尚未裁定。**

人工盲標樣本已用固定種子 `20260911` 從 225 個 clean 群分層抽出 58 群，
並排除已看過的 7 群。檔案在 `_rescued_scratchpad/blind_label_sample.csv`，
只有 `group_id` 與 opaque `stratum`；抽樣程式是
`src/classify_lite/sample_blind_labels.py`。盲標模式只讀其中的 `group_id`，
再用獨立顯示種子 `2026091101` 打散；不讀、不顯示分層或任何模型判定內容。
紀錄寫入 `_rescued_scratchpad/blind_labels.csv`，每批 10 群、一次最多 3 批。
目前已有 30 筆 v1 手填紀錄，7 筆違反 must，原檔保持不動；修正 must 後的新
紀錄預留於 `_rescued_scratchpad/blind_labels.v2.csv`。版本摘要在
`_rescued_scratchpad/blind_labels_versions.json`。`ref/label_dictionary.csv` 的
五個 disposition 列之 `具體例子` 目前都是 `—` 佔位符，例子由人補寫，代理不得代填。

---

## 已經撞過的牆

**這一節是為了不要重走一遍。** 五件事都量過。

**一、判定器對值域改動不穩定，而且比看起來更不穩。**
同指紋、同 23 群、重跑兩次，`disposition` 的成對一致率 **82.6%**；
三批全同只有 **16/23**。而 16 是**上界**——一個「每四次翻一次」的群，
三次全同的機率是 0.44，所以三次全同會系統性高估穩定度。

**二、23 群的測試台驗不動小效果。**
跨指紋的雜訊底線實測 **8.7pp**，而 n=23 的判讀門檻（一群 4.3pp，
變動要跨兩群）**正好也是 8.7pp**。兩個數字相等不是保守估計。
擴到 n=46 門檻降到 4.35pp，仍被 8.7pp 的擺動蓋過。

**三、驗證比被驗的東西還貴。**
要穩妥驗一到兩群的效果需要 69 群的測試台，代價 **$6.88**；
而直接跑完剩下的 202 群是 **$4.01–6.05**。

**四、所以方案改成：直接跑未判定的 118 群，比分布，不比逐群一致率。**
118 群是唯一符合 `ref/annotation_protocol.md` 第二節 (c) 的乾淨驗證集
——完全未判定，沒有選擇偏誤。

**這個方案有一個無法用加樣本解決的限制。**
118 群只涵蓋 clean 群請求的 **24.2%**（佔分類母數 **13.5%**）。
請求量集中在已污染的 A001–A023（佔 clean 群請求 62.5%），
clean 前三大群 `A001`（7,503 筆）、`A002`（4,753）、`A013`（2,687）
**全部在測試台裡**，118 群裡最大的只有 1,940 筆。
**以請求加權的效果在乾淨資料上驗不出來，這不是 n 不夠。**

**五、全群跑完後，正例沒有讓現行判準收斂。**
225 個 clean 群已在 `e13f9738…` 下跑完；結果見上面的〈現行分流結果〉。
四個既知 `tool_injected` 候選只命中兩個、24 個高隱藏上下文候選沒有任何一個
落 `insufficient`、`batch_project` 的 uid ≤ 3 符合率也只有 54.0%。
這些是下一輪裁定值域／判準的材料，不是修改完成的證據。

---

## 凍結座標

四個座標。**這裡只放值與代價，完整定義在 `ref/annotation_protocol.md` 第一節。**

不寫「請勿修改」——總有一天會有正當理由要改。要知道的是改了什麼會作廢。

| 座標 | 值 | 它變了，什麼東西作廢 |
| --- | --- | --- |
| 母體指紋 | `63af7d40…`（17,365 個 `prompt_text_sha256` 排序後 `"\n".join` 再取 sha256） | 分類母數換人，**所有以 17,365 或 58,100 為分母的比例全部失效**，而且是靜默的——比例照樣算得出來 |
| 長度切點 | `[251, 986, 4799]` | 24 個分層的成員換人；`baseline.py` 的門檻也是這個 Q1，基準線跟著飄 |
| 聚類參數 | 門檻 10、階梯 `[4000, 2000, 1000, 500, 250, 120, 60, 30]`、前綴上限 4000 字元 | **群集清單整個換掉。`group_id` 不再指同一批內容**，所有既有判定與審閱紀錄作廢。門檻 20 的 `C01` 有 987 個相異內容、門檻 10 的 `A001` 有 974——**編號看起來對得上，內容不是同一批** |
| 提示詞指紋 | `e13f9738…`（完整值見〈現行分流結果〉） | `judge_output.e13f9738.csv` 的 225 列全部作廢，須全量重跑。改動它要跟重跑一起做，不是改個數字讓測試變綠 |

指紋涵蓋：提示詞模板本文、三軸值域與定義、模型名稱、送進提示詞的中繼資料
欄位清單、前綴上限與送哪一欄前綴、執行路徑、旗標集合、json-schema、
盲化探針的問法與值域。**三軸定義取自載具的 `FIELDS`／`FIELD_HELP`，
不是 `ref/label_dictionary.csv`**——判定器不讀那個 csv。

---

## 執行環境

- Windows，專案根目錄 `C:\Users\isrea\Desktop\AIR\codex用戶分析\資料探勘v2`
- Python，直接跑，不使用容器
- 原始資料在 `data/00_raw_lite/`（680 MB，全部 gitignore）
- 群集判定的工作檔在 repo 外，見〈repo 外的工作檔〉
- 測試：`pytest tests/`。**通過數以實際輸出為準，這裡不記具體數字**——
  手工維護的計數每一輪都會過期（444 → 456 → 470，兩輪內錯兩次），
  而一個永遠對不上的數字會訓練人忽略它

---

## 絕對禁止

1. **明文 `prompt_text` 不進 parquet、不進 git、不進任何衍生表。**
   只存 `prompt_text_len` 與 `prompt_text_sha256`。內容含臨床照護紀錄。
   `markers.check_columns()` 有硬斷言擋住，不要繞過。

2. **不要讓 AI 代理執行 `review_clusters.py` 或 `rescreen_prefixes.py`。**
   它們把完整內容印到螢幕，而**經過 AI 的 tool output 會寫進
   `~/.claude/projects/*.jsonl`**——等於明文落地。
   `judge_clusters.py` 已改為**有條件允許**（閘門硬性只處理 clean 群、
   不 print 前綴／`prompt_text`／`reason`、輸出只有計數與值域標籤，
   三條由 `OutputGuard` 機械保證）。那兩支沒有這些保證，**理由未消失**。
   看內容用 `python review_clusters.py --inspect A008,A010`（只顯示、不寫檔），
   由**人**在自己的終端跑。

3. **不要從模型的判定結果導出分類法的邊界。**
   一本從模型行為抄來的字典，會讓之後**每一群**的人工判定都不獨立，
   而算出來的一致率與真正的一致率**在數字上完全一樣**。
   已污染的七群（A008／A010／A013／A017／A018／A019／A020）與完整規則見
   `ref/annotation_protocol.md` 第二節〈分類法的定義不得從模型的判定結果導出〉。

4. **不要把 lite 檔案放進 `data/00_raw/`（含子目錄）。**
   `extract.py` 用 `DATA_RAW.rglob("*.json")` 掃檔，會用 clean 的欄位路徑
   解析 lite 檔，產出 12 萬列近乎全 null 的資料而不報錯。
   `config.lite_raw_dir()` 有防守。兄弟目錄 `data/00_raw_lite/` 安全。

5. **不要跑 `git clean -xfd`。** `-x` 連 gitignore 的檔案一起刪，
   `data/` 底下有 680 MB 原始資料。用 `git clean -fd` 或先跑 `-xfdn` dry run。

6. **不要在 `ref/` 建新檔案而不檢查 `.gitignore` 白名單。**
   那裡是白名單制（`ref/*` 擋全部、逐項放行），被 ignore 的檔案不會出現在
   `git status`，新檔就這樣安靜留在本機直到有人 clone 之後點不到。
   檢查指令：`git check-ignore -v ref/<新檔名>`

7. **不要放寬 `render_index.run()` 的 `line == "clean"` 斷言。**
   它擋的是呼叫端傳錯，而 `render_index` 有四處直接讀全域 `REGISTRY`，
   `list_metrics(line)` 攔不住。兩道防線各管一件事。

8. **重跑成本必須走 `load_dataset()`，不能自行 glob 拼接 parquet。**
   後者少一個 `date_taipei` 分區欄（Hive 分區不在檔案裡），
   生效日分段會靜默挑錯價目，總額差幾塊錢而不報錯。

---

## 驗證方式

每一輪改動之後的硬性驗收，四項全過才算完成：

```
clean 的輸出檔逐位元組不變
  docs/data/ 的 csv  +  docs/RESULTS.md  +  docs/figures/ 的圖
  數目以渲染時印出來的為準（publish 那一行會報），對不上就是這裡過期了
渲染冪等——連跑兩次「無變動」
pytest 全過（不記具體數字，以實際輸出為準）
成本總額 $2,749.32
```

**`concentration.csv` 不在裡面，它在 `runs/<run_id>/`，不進版控。**
先前這裡把它列進 `docs/` 那一串，讓清單加起來與寫的數字對不上——
兩個都錯，卻因為沒有人相加而存活了下來。

「逐位元組不變」不是形式。`concentration.csv` 決定每個指標的哪些格子被抑制，
改壞了不會報錯，只會讓本該遮蔽的數字流出去或反過來擋掉該出的數字——
這種錯誤沒有測試抓得到，只能靠逐字比對。它自己雖然不在驗收清單裡（每次執行
產生新的一份），它的影響會出現在 `docs/data/` 的 csv 上。

---

## repo 外的工作檔

**全部在 `_rescued_scratchpad/`（repo 的兄弟目錄），不進版控，換機器要另外帶。**
下面按「沒了會怎樣」分三級。

### 凍結產物——沒了要重跑，而重跑會讓一切對不上

| 檔案 | 內容 |
| --- | --- |
| `clusters.parquet` | 226 群的群層中繼資料 |
| `cluster_members.parquet` | 每個相異內容一列，對到 `group_id` |
| `cluster_prefixes.parquet` | 每群的共同前綴（**含明文，不可 print**） |

**重跑聚類會換掉 `group_id`。** 那不只是重算——`prefix_screen.csv`、
`judge_output*.csv`、`pipeline_groups.csv`、`docs/CLASSIFICATION_NOTES.md`
裡所有以群編號寫的東西**全部對不上，而且不會報錯**。
見〈凍結座標〉的聚類參數那一列。

### 唯一來源——沒了不可重建

| 檔案 | 為什麼不可重建 |
| --- | --- |
| `prefix_screen.csv` | 226 群的**人工**個資篩檢（225 clean / 1 flagged）。重建要人再看一遍全部前綴 |
| `prefix_rescreen.csv` | 抽樣複篩的自我一致率紀錄（20/20） |
| `review_clusters.py` | **三軸值域與定義的唯一來源**（`FIELDS`／`FIELD_HELP`）。`judge_clusters.py` 用 `_load_carrier()` 從它取，不另寫一份——兩邊定義不同就沒得比 |
| `judge_clusters.py` | 判定器。指紋由它與載具共同決定 |
| `rescreen_prefixes.py` | 複篩抽樣程式（含分層保底規則） |

`ref/label_dictionary.csv` 裡「層 == 分流判準」那些列必須與 `FIELD_HELP`
**逐字相同**，由 `tests/test_judge_clusters.py` 的同步測試保證。
**兩邊漂掉不會報錯**：判定器照舊用舊值域，文件寫著新值域，輸出檔看不出差別。

### 中間檔——可重生，但要知道用什麼重生

| 檔案 | 怎麼來的 |
| --- | --- |
| `judge_output.csv` | `judge_clusters.py` 的預設輸出（現有 23 列，指紋 `a0d19daa…`，**作廢**） |
| `judge_output.79635e2b.csv` | 107 列，舊指紋，**作廢**。保留作雜訊底線的歷史量測 |
| `judge_output.a0d19daa.rerun1.csv` | 23 列，同指紋重跑的第二次，**作廢**。同上 |
| `judge_output.a2dc7b11.constraint_absent.csv` | 118 列，未帶軸間約束的舊指紋，**作廢**。只作歷史比較 |
| `judge_output.83086cd2.csv` | 118 列，未帶隱藏上下文欄的舊指紋，**作廢**。其群編號集合用來界定主 cohort |
| `judge_output.e13f9738.csv` | **現行結果**：225 個 clean 群各一列；1 個 flagged 群不在其中 |
| `pipeline_groups.csv` | 226 列，`group_id` → `pipeline_id`。**材料，不是結論**——多 uid 的 137 群併法未定，`pipeline_id` 留空 |

---

## 名詞

| 詞 | 意思 |
| --- | --- |
| clean / lite | 兩批資料的 schema 代號，上游取的。不是「乾淨」與「輕量」的形容 |
| 識別碼 / uid | `anonymous_user_id`，人層級的唯一鍵 |
| 遮罩帳號 | `user_account`，末三碼被替換成 `XXX`。編碼學位＋入學學年＋系所，**多對一**，只能當屬性來源不能當識別碼 |
| dept_key | 系所身分。同系所在不同學制用不同代碼（生醫 09/01、醫工 26/31、人工智慧 28/61），此欄讓它們歸為同一個系所 |
| 可歸屬 | 能對應到自然人的請求，60,445 筆。服務憑證不算 |
| 服務憑證 | `link-*`、`open*` 形式的程式帳號，82 個，佔 49.85% 請求。**非自然人** |
| 前置規則 | 四條用中繼資料就能判定、不需送分類器的規則 |
| 牌價等值成本 | token 用量 × 公開牌價。**不是實際帳單，也不是全量** |
| 分流判準 | `frame_owner`／`disposition`／`axis_level`，判「這一群要不要進母數」。給判定器用 |
| 分類法 | `invocation`／`role`／`domain`／`status`，判「這次呼叫是什麼用途」。給人標註用，判定器看不到 |

---

## 兩批資料

| | clean（7 月） | lite（8 月） |
| --- | --- | --- |
| 期間 | 07-21 至 07-23，1.8 天 | 07-30 至 08-24，24 天 |
| 筆數 | 9,937 | 120,520（**上游過濾後**，見〈關鍵數字〉） |
| 識別碼 | `username`，本地序號，98 個 | `anonymous_user_id`，雜湊，357 個 |
| 對話結構 | **有 `turn_id`** | 無 |
| 內容欄位 | `content.prompt` / `content.memory` / `content.assistant_response` 三段分開，另有 `instructions_length` | **只有 `request.prompt_text` 一欄** |
| 遮罩帳號 | 50 個 | 119 個（交集 44） |

**不可合併成連續序列**，三層原因：識別碼分屬兩套不相干的命名系統；
期間不連續（07-24 至 07-29 缺）；只有遮罩帳號層級對得起來，而那是多對一。

**clean 不是「舊的那份」。** 它有 `turn_id`，是唯一能講對話內部機制的資料
（快取位置效應、turn 展開、token 膨脹中位數 52 倍）。lite 因為 schema
沒有 `turn_id`，那三個指標**永遠做不出來**。
**它也是唯一能看到 `prompt_text` 涵蓋了什麼的資料**——見〈關鍵數字〉。

---

## 關鍵數字

全部已查證。任何數字有疑問時以 `docs/data_lite/` 的 csv 為準。

```
規模      120,520 筆、357 個識別碼（學生 155 + 教職員 120 + 服務憑證 82）
人數      275（上界，遮罩碰撞使實際只會更少）
可歸屬     60,445 筆 = 50.15%

成本      US$2,749.32（取價 2026-08-27）
四段       未快取 30.73% / 快取讀 28.46% / 快取寫 18.76% / 輸出 22.05%
快取合計   47.22%
可靠度     confirmed 78.43% / inferred 13.55% / promotional 7.05% / provisional 0.97%
          inferred 全部來自 gpt-5.6-sol 08-22 降價前的快取寫入推估價（$372.40）
88.30%    的 prompt token 是快取讀取

分類母數   120,520 → 33,666 相異內容 → 31,011（≥20 字元）→ 17,365
前置規則   向量化 587 / 影像 401 / 工具自動發出 2,387 / 流程中繼 10,271
          四項合計 13,646，13,646 + 17,365 = 31,011

單位分布   教職員 120 人／22,524 筆    工學院 65／13,814
          管理學院 33／12,086         智慧運算 24／8,073
          醫學院 33／3,948

成本分布   教職員 $1,054.95   服務憑證 $495.67   工學院 $445.11
          管理學院 $354.94   智慧運算 $264.46   醫學院 $134.18
          六項相加 = $2,749.31，與總額差 0.01（分項四捨五入）

集中度     最大遮罩帳號佔比：管理 78.69% / 智慧運算 61.14% / 工 30.90% / 醫 16.79%
每筆成本   教職員 $0.0468 是服務憑證 $0.0083 的 5.68 倍
          = 本地模型稀釋 3.16× × 模型選擇 1.80×

營運事件   73.12 小時服務中斷（台北 08-07 13:23 至 08-10 14:30）
          756 次限流，全部落在 08-18 15:00 至 08-19 14:00 的單一窗口
```

### 三項會改變數字意義的界線

**這三項不改任何已發佈的數字，但改變它們的意思。** 新對話若不知道，
會把它們當成比實際更完整的答案。

**一、上游的前置過濾——`120,520` 不是 gateway 的流量。**

```
source_files 141,619 → written_records 120,520 + excluded_internal_prompts 21,099（14.90%）
排除集中在兩天：07-30（68.1%）、08-03（73.8%），其餘 20 天為 0
```

所以 `US$2,749.32` 除了「牌價等值非帳單」之外，**還要加一句「且非全量」**；
日曲線有兩天被截斷，其中一天是資料的第一天。
**缺口的大小連上界都給不出來**（被排除記錄的 token 用量完全未知）。
→ 詳見 `docs/UPSTREAM_FILTER.md`，第六節有七個待問上游的問題。

**二、可分類性上限——我們分類的是使用者敲進去的那一段，不是整個請求。**

```
clean 側（量得到）  59.26% 的請求另帶 instructions 或 memory
                    那些筆裡 prompt 佔三段總長的中位數 0.5%
lite 側（只有下界） prompt_tokens > 4 × prompt_text_len：佔請求 71.93%、佔相異內容 40.02%
                    K 從 1 到 8 幾乎不動，所以不是分詞器邊界效應
```

`17,365` 這個數字沒有錯；被高估的是**可分類性**。
**clean 的 59.26% 不可轉移到 lite**——兩批期間與母體不同。
→ 詳見 `ref/annotation_protocol.md` 第一節〈可分類母數 17,365 的可分類性上限〉。

**三、群數不等於流水線數。**
單一 uid 的 89 群，依「同一 uid 且時間重疊」合併之後只有 **39 條流水線**
——**群數高估 56.2%**。（另有 137 群是多 uid，併法未定。）
所以「做什麼」的答案，其分母比群數更接近 39 那一側。
→ 材料在 `_rescued_scratchpad/pipeline_groups.csv`。

---

## 工作方式

這是交接時最不明顯、也最該傳達的部分。

**分工**：對話端負責設計方向、解讀結果、產生提示詞；Claude Code 執行實作與
查證，回報後對話端再判讀。

**每一輪的提示詞都帶硬性驗收條件**，而且明確要求：

> 所有數字一律以檔案為準，對不上的直接改並告訴我改了什麼。

這一條不是客套。對話端從歷史取數字寫錯過多次，Claude Code 的查證是主要的
糾錯機制——**它抓出錯誤是設計，不是意外**。**指令裡的前提也要查證**：
本專案發生過多次「照我上面給的數字」而那個數字是錯的或根本沒出現。

**做重要改動之前先盤點，不要直接改。** 有一次「反轉抑制預設值」看起來明顯
正確，盤點之後才發現單獨反轉會讓 4 個指標從「明確標示豁免」掉進
「靜默無保護 + 誤導的警告」，必須三件事同時到位。

**寫測試之後跑突變測試**——故意把程式改壞，看測試會不會叫。專案靠它抓到過
「空真」（斷言在集合為空時自動成立，看起來通過但什麼都沒驗），
也抓到過文件裡兩張表用了不同分母而並排。

**先立不變量，再改語意。** 要改一個兩邊必須一致的東西時，先讓兩邊在**現況**
下被測試釘住，再動內容——這樣改的動作發生在保護底下，而不是先改完再補測試。

**commit 的署名依執行環境的 session 設定，不依對話端的偏好。**
執行環境若明寫「這取代先前的署名指示」，就以環境為準；兩邊衝突時
**回報一次即可，不要每次 commit 都問**。既有的 commit 不重寫——
重寫歷史換來的一致性，代價比不一致本身大。

---

## 反覆犯過的錯

全部發生過不只一次。**類數不記**——這一節會長，而記了就會過期。**判準是「新對話在不知情下會不會再犯」**，
只在這裡收會重演的。

**一、從對話歷史取數字，而不是從檔案。**
`90.3%` 已經在 `PROGRESS.md` 修正成 `88.30%`，寫第三節時又寫回去——因為歷史裡
兩個版本都在，舊的還比較早出現。**修正過的東西會退回去。**

**不只是對話歷史，從另一份文件取數字也算。** 本檔的成本分布一度寫成
工學院 $411.00 / 智慧運算 $202.13 / 醫學院 $88.30，來源是
`DESIGN_NOTES` 的價目變更前後對照表，而那是另一個計價情境的對比，
不是已發佈的總額。
**唯一的判準是「這個數字有沒有出現在 `docs/data*/` 的 csv 裡」**——
散文、筆記、對照表都不算，即使它們在 repo 裡。

**二、憑印象造數字。** 「24 個分區有 21 個不完整」是編的，實際是 22 個完整、
2 個結構性截斷。而且錯的方向是低估資料品質，會讓後面所有數字被無端打折。

**三、引用不存在的東西。** `DESIGN_NOTES` 引用 `markers.py` 為專案模組，
但那是 scratchpad 的一次性腳本，從未進版控。同一件事第二次發生在
`ref/prefilter_markers.csv`——文件已連結它，而它被 gitignore 擋著。

**四、數字寫進文件前未查證。** 「醫護人員出現 4 次」（實際 2 筆）、
「27 個系所身分」（實際 25）、「13,643」（用一個多算 3 的數字反推的）。
**拿百分比反推回筆數也算沒查證**——`UPSTREAM_FILTER` 的規則 4 表格
就這樣錯了兩格。

**五、分層抽樣的保底名額寫成「先給再扣」。** 三次，都在抽樣程式裡，
都不報錯：`valset.py` 的 `max(1, ...)` 被 `break` 吃掉、
`rescreen_prefixes.py` 的超配回收把保底扣回 0、
同一支改寫後把斷言放在函式出口而被補足迴圈意外「修好」。
**失效條件是「小層」，而小層正是保底存在的理由。**
完整規則見 `docs/ENGINEERING_NOTES.md`〈保底寫成先給再扣就不是保底〉。

**六、在資料支撐不足的地方，補一個聽起來合理的機制。**
三次，全部由對話端提出、執行端量測後推翻：

- 「兩組邊際相同」被推論成「兩件事完全共現」——實際是抽樣設計的產物
- 「+4.3pp」被讀成改動的貢獻——那是一個群，剛好等於解析度
- 「大群比較容易不穩定，所以選擇偏誤把大群一起帶走」——實際是**群編號按
  相異內容數遞減指派**（ρ=−0.999），「先跑 A001–A023」等於「先跑最大的 23 群」

**共同形狀：機制講得通、方向也對，但差在量測解析度之內。**
規則見 `docs/ENGINEERING_NOTES.md`〈差小於量測解析度時，不要從方向讀出結論〉。

**七、用 heredoc 把含跳脫字元的程式碼餵給 python。**
三次，第三次的形態最壞：bash 把 `\\` 吃成 `\`，正規式
`r"\n\s*"` 於是變成 `r"n s*"`——**Python 只發一個 `SyntaxWarning`，
程式照跑，測試照綠**，只是比對的東西換了。前兩次是 `\n` 塌成真的換行、
`$2` 被當成位置參數展開成空字串（`US$2,749.32` → `US,749.32`）。

**規則：不要用 heredoc 傳含跳脫字元的程式碼。先寫 `.py` 檔再執行。**
非跑不可時用**加引號**的 heredoc（`<<'PY'`），但那擋得住 `$` 展開、
擋不住反斜線塌陷——所以那只是次選，不是解法。

**共同形狀：壞掉的結果仍然是合法的輸入。** 少了一個反斜線的正規式仍然
是正規式，少了 `$2` 的句子仍然通順。逐格驗數字的測試抓不到，
因為它驗的是「有沒有值」，而值一直都在。

---

## 已知限制

寫在 `docs/OVERVIEW.md` 第六節，完整版看那裡。

- **粒度**：最小可靠層級是學院。遮罩替換帳號末三碼，對學生那是班級序號，
  故同系同屆的人分不出來（119 個遮罩帳號有 39 個對應多個識別碼，最多 29 個）。
  方向單向：**不同遮罩帳號可確定非同一人，同一遮罩帳號無法確定為同一人。**
- **成本**：牌價等值、非帳單、且非全量。四項系統性差異（service_tier、
  區域加價、機構折扣、促銷價時效）皆無法自資料判定。
  促銷價官方標示至少維持至 2026-11-21。
- **期間**：24 天全在暑假。
- **可分類性**：見〈關鍵數字〉第二項。**這一項與前三項不同級**——
  前三項限制的是數字的精度，這一項限制的是問題本身能不能被回答。
- **可改善**：`turn_id`、教職員的單位資訊、`service_tier`、
  以及上游若保留 `system`／`instructions`。最後一項的鬆動幅度最大：
  **不是回復部分請求的用途，是換掉分類的對象**——屆時要重做，不是補做。

---

## 治理發現

不在原始提問範圍內，但可能是最重要的部分。寫在 `OVERVIEW` 第五節。

日誌中包含多類第三人資料——病人臨床紀錄（含意識狀態、生命徵象、給藥、管路）、
具名醫護人員、教職員人事處置、持續累積的個人側寫、學生同儕的通訊軟體對話、
可定位的住戶設備位址。**其當事人均非本 gateway 的使用者，未曾亦無從提供同意。**

另有兩條**使用者沒有輸入、也未必知道**的管道：校內服務的存取權杖隨客戶端
自動附加的瀏覽器狀態進入日誌；AI 編碼客戶端的回合交接摘要把使用者工作區
路徑與專案結構送進 gateway。

**寫作原則**（重要，任何要動這一節的人都要遵守）：
只寫結構事實、不引述內容、不間接指認、陳述不建議。

在「一個識別碼即可構成一支流水線」的母體裡，**形態描述本身就是準識別資訊**
——說出「某人在做 A 的同時也在做 B」，不含姓名也足以指認。
`docs/CLASSIFICATION_NOTES.md` 因此不進版控。

---

## repo 結構

```
src/          管線。clean 與 lite 兩條線平鋪，共用模組無前綴
  extract.py / extract_lite.py       L0 → L1
  schema.py / schema_lite.py         契約驗證
  aggregate.py / aggregate_lite.py   L1 → L2
  metrics/ / metrics_lite/           指標
  college.py / pricing.py            共用的判斷邏輯
  classify_lite/                     用途分類前置（markers、prefilter）
  render_*.py                        文件渲染

ref/          人工維護的對照表（白名單制 gitignore，新檔要先 check-ignore）
  college_mapping.csv       進版控   系所代碼 → 學院
  pricing_table.csv         進版控   模型 → 牌價
  prefilter_markers.csv     進版控   前置規則判定字串
  label_dictionary.csv      進版控   兩層：分類法（給人）與分流判準（給判定器）
  annotation_protocol.md    進版控   凍結座標、標註規則、退回門檻
  user_registry.csv         gitignore（含帳號）
  lite_user_registry.csv    gitignore（含帳號）

docs/
  OVERVIEW.md               主要交付物，七節
  UPSTREAM_FILTER.md        上游過濾了什麼（影響規模與成本）
  RESULTS_lite.md           lite 完整表格
  RESULTS.md                clean 完整表格，含 turn 層分析
  INDEX.md                  指標索引：分母、注意事項、抑制狀態
  RUNBOOK_lite.md           資料擴充與重跑的操作手冊，含已知的坑與本批基準值
  PROGRESS.md               階段性進度快照（保留，勿刪）
  ENGINEERING_NOTES.md      通用工程規則，公開（條數不記，同樣會過期）
  DESIGN_NOTES.md           gitignore，專案特定判斷與未定稿內容
  CLASSIFICATION_NOTES.md   gitignore，逐筆形態描述（準識別資訊）
  data_lite/ figures_lite/ data/ figures/

data/         全部 gitignore
runs/         執行快照，gitignore
```

**`src/` 兩條線平鋪不要重構。** 痛感還不夠，而搬檔會讓 `git blame` 斷掉、
`import` 全改、整套測試要重驗。觸發條件：出現第 4 個共用模組，
或第一次有人拿錯檔案。

---

## 管線指令

```bash
python -m src.extract_lite                      # L1：原始 JSON → parquet
python -m src.schema_lite                       # 契約驗證
python -m src.run aggregate                     # L2：人層級表與集中度
python -m src.run metrics --line lite --publish # 指標，並發佈到 docs/
python -m src.run metrics --line clean --publish  # 驗收用：clean 逐位元組比對

python -m src.classify_lite.markers             # 分類前置：掃結構標記（約 3.5 分鐘）
python -m src.classify_lite.prefilter           # 分類前置：套四條規則（秒級）
python -m src.classify_lite.sample_blind_labels # 固定種子重建 58 群人工盲標樣本
```

最後三支不在主管線，單獨執行。詳見 `docs/RUNBOOK_lite.md` 情境三。

群集判定的指令在 `_rescued_scratchpad/`，由**人**執行（見〈絕對禁止〉第 2 條）：

```bash
python review_clusters.py --inspect A008,A010   # 只顯示，不問不寫
python review_clusters.py --blind-label         # 58 群人工盲標：獨立亂序、分批、可續跑
python review_clusters.py                       # 完整審閱：三軸判定，寫 cluster_review.csv
python review_clusters.py --screen              # 前綴個資篩檢
python judge_clusters.py                        # LLM 判定（AI 代理可執行）
```

---

## 未處理的項目

依優先序。

1. **問上游關於 `excluded_internal_prompts` 的七個問題**
   （`docs/UPSTREAM_FILTER.md` 第六節）。
   **最優先，因為答案可能改變後面的優先序**——若被排除的記錄拿得到中繼資料，
   成本與規模的缺口就補得起來；若它也套用在 `prompt_text` 之內，
   整個分類路線要重想。
2. **人工盲標尚未執行。** 58 群樣本已凍結並寫入 repo 外工作檔；由人在
   `review_clusters.py --blind-label` 模式中盲標。載具只從樣本讀 `group_id`，
   不得接觸模型判定或分層。
3. **分流值域／判準尚未收斂。** 225 個 clean 群是描述性開發資料，且依
   預先登記的 default 共現門檻判為不可信；修法要等獨立人工盲標結果。
4. **三軸內容分類未開始**（`invocation`／`role`／`domain`／`status`）。
5. **84 群雖已有現行與舊指紋配對，跨指紋變動尚未量。** 兩次之間同時改了
   不只一件事，所以只能當加值描述，不能解讀成單一改動的效果。
6. **全 225 群的生產探針失敗率無法從產物重建。** 判定 CSV 不存探針事件，
   先前批次也沒有完整持久化日誌；2026-09-11 最後 31 群的有效執行是 0/2
   失敗。若之後要報全批率，必須先讓執行器把每次探針結果另存安全計數。
7. **`docs/OVERVIEW.md` 有五件待處理，必須一起改不可單獨更新**
   （部分更新比全部過期更難追）。清單在 `DESIGN_NOTES`；
   第五件要等上游回覆才能定稿。
8. **有 commit 未 push，tag `docs-lite-2026-08` 也未推。**
   數目以 `git log --oneline origin/main..HEAD` 為準，這裡不記。
9. **`DESIGN_NOTES` 與 `CLASSIFICATION_NOTES` 不在 repo 裡**——換機器要另外帶，
   連同整個 `_rescued_scratchpad/`。
10. **`pricing.py` 保留的舊值對照無法重現**——需同時回退價目表，但沒有記錄
   該回退到哪個版本（刻意不補 tag，那兩個數字的用途是對比不是重跑）。
11. **兩條 relay 標記 100% 共現的觀察沒有自動檢查**，靠 RUNBOOK 提醒。

---

## 記錄位置

| 內容 | 位置 |
| --- | --- |
| 取得新驗證資料前的凍結版本 | `ref/FREEZE_2026-09.md` |
| 凍結座標、標註規則、退回門檻、判讀規則 | `ref/annotation_protocol.md` |
| 上游過濾了什麼（影響規模與成本） | `docs/UPSTREAM_FILTER.md`（公開） |
| 通用工程規則（可外帶到別的專案） | `docs/ENGINEERING_NOTES.md`（公開） |
| 專案特定的設計判斷、歷史敘述 | `docs/DESIGN_NOTES.md`（不公開） |
| 標註過程的逐筆觀察 | `docs/CLASSIFICATION_NOTES.md`（不公開） |
| 操作手冊與已知的坑 | `docs/RUNBOOK_lite.md` |
| 每個指標的分母與但書 | `docs/INDEX.md` |
| 執行快照（哪次跑的、程式哪個版本） | `runs/<run_id>/`（不進版控） |

**成本的重現座標是 tag，不是 commit。** 成本是「資料 × 外部價目 × 取價時間」
的函數——這是全專案唯一「資料沒變但數字會變」的指標。要重現 $2,749.32
需要 `docs-lite-2026-08` 那個 tag 底下的 `ref/pricing_table.csv`。
